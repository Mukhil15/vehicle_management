const ApproveForm = ({ request }) => {
  return (
    <div>
      {/* Display request details and form for approval */}
      <p>Request Details for Approval: {JSON.stringify(request)}</p>
      {/* Implement the approval form logic here */}
    </div>
  );
};

export default ApproveForm;
